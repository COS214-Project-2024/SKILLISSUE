*****************
TO RUN THE TESTS
*****************

Ensure CMake is installed.
Ensure googletest is installed.
Ensure you are inside the ResourceManagerTesting folder in your terminal.
Make a directory build (or first remove the old (rm -rf build)):
    mkdir build
Enter the directory:
    cd build
Run the following:
    cmake ..
    make ResourceManagerExe
    ./ResourceManagerExe

